import React from "react";
import { Link } from "react-router-dom";

const Brandlink = ({ classes }) => (
        <Link className={`text-2xl font-font-krona ${classes}`} to="/">
            Welly
            </Link>
);

export default Brandlink
